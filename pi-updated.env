# FastRTC Gateway Connection
FASTRTC_GATEWAY_URL=ws://192.168.1.2:8080  # For Ethernet
# OR
# FASTRTC_GATEWAY_URL=ws://192.168.1.17:8080  # For Wi-Fi

# Authentication
AUTH_TOKEN=
DEVICE_ID=rpi-zero2w-001
TOY_ID=kd729cad81984f52pz1v1f3gh57q3774

# Voice Recognition
VOSK_MODEL_PATH=/home/pommai/models/vosk-model-small-en-us-0.15
ENABLE_WAKE_WORD=false

# Cache and Storage
CACHE_DB_PATH=/home/pommai/cache/pommai_cache.db
AUDIO_RESPONSES_PATH=/home/pommai/audio_responses

# Offline Mode
ENABLE_OFFLINE_MODE=true

# Audio Configuration
AUDIO_SEND_FORMAT=pcm16  # Format for recording audio (Pi to Gateway)
PLAYBACK_SAMPLE_RATE=48000  # 48kHz for Bluetooth A2DP (matches ElevenLabs pcm_48000)
RESPEAKER_LED_COUNT=2

# SPI Configuration
SPI_BUS=0
SPI_DEV=0

# Audio Device - Comment out to auto-detect
# POMMAI_FORCE_OUTPUT_DEVICE_INDEX=5  # Commented out - let system auto-detect

# PCM Configuration
PCM_SWAP_BYTES=false
