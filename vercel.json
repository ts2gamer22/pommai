{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "turbo run build --filter=@pommai/web",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": "nextjs",
  "rootDirectory": "apps/web"
}
